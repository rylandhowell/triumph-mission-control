- User wants a Linear-style “Mission Control” web app for Triumph Homes.
- Hard requirements captured from chat:
  - Clean Linear-like interface.
  - Strong calendar page as a core feature.
  - Calendar tab must support a calendar for each active house/job.
  - Master calendar with per-house filtering/toggle (All Houses vs single house), color-coded by house/event type.
- User asked about background work and sub-agents; requested sub-agent “Henry” to build in parallel while assistant remains available.
- User requested logo handling requirements:
  - Remove background (transparent logo).
  - Export SVG.
  - Dark/light-safe variants.
  - Simplified icon mark for sidebar/app icon.
- User wants a way to monitor sub-agent progress (“Henry Status” panel with step status, changed files, percent complete, preview/run status, next queue).
- At session end, user reports localhost:3000 cannot connect in Safari; likely dev server not running or wrong directory/start command.
